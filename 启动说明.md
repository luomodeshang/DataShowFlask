# 项目启动说明

## 已修复的问题

1. ✅ **修复 math 模块导入问题**
   - 在 `app.py` 文件顶部添加了 `import math`
   - 移除了重复的导入语句

2. ✅ **修复数据库配置文件路径问题**
   - 支持在多个位置查找配置文件（根目录和 YieldModel 目录）
   - 添加了 UTF-8 编码支持

3. ✅ **修复 Windows 中文乱码问题**
   - 在 `app.py` 和 `start_backend.py` 中添加了编码设置
   - 自动设置控制台代码页为 UTF-8
   - 设置标准输出编码为 UTF-8

## 启动方式

### 方式一：手动启动（推荐）

#### 1. 启动后端服务

在项目根目录下运行：

```bash
python app.py
```

或者使用启动脚本（会显示更多信息）：

```bash
python start_backend.py
```

后端服务将在 `http://localhost:5000` 启动

#### 2. 启动前端服务

打开新的终端窗口，进入前端目录：

```bash
cd big-screen-vue-datav
npm run serve
```

前端服务将在 `http://localhost:8080` 启动（通常是8080端口）

### 方式二：使用启动脚本（Windows）

直接运行：

```bash
start_services.bat
```

这会自动启动前后端服务。

## 访问地址

- **前端大屏**: http://localhost:8080
- **运维平台**: http://localhost:8080/maintenance
- **后端API**: http://localhost:5000

## 注意事项

1. **Python 依赖**: 确保已安装 `requirements.txt` 中的所有依赖
   ```bash
   pip install -r requirements.txt
   ```

2. **前端依赖**: 确保已安装 npm 依赖
   ```bash
   cd big-screen-vue-datav
   npm install
   ```

3. **数据库配置**: 
   - 如果使用真实数据库，确保 `新建文本文档.ini` 或 `YieldModel/新建文本文档.ini` 配置正确
   - 如果数据库不可用，系统会自动使用模拟数据

4. **模型文件**: 
   - 模型文件位于 `trained_models/` 目录
   - 如果模型文件不存在，系统会使用模拟数据

## 测试

运行测试脚本检查服务是否正常：

```bash
python test_backend.py
```

## 常见问题

### 1. 端口被占用
如果 5000 或 8080 端口被占用，可以：
- 修改 `app.py` 中的端口号
- 修改 `vue.config.js` 中的代理配置

### 2. 模块导入错误
确保已安装所有依赖：
```bash
pip install -r requirements.txt
```

### 3. 中文乱码
已自动修复，如果仍有问题，请检查终端编码设置。




