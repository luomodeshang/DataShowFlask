# 数据库集成说明

## 已完成的工作

### 1. ✅ 创建数据获取模块 (`data_fetcher.py`)

已成功将 YieldModel 中的数据获取逻辑移植到主项目中，包括：

- **Connect_SQL**: 连接SQL Server数据库并获取原始数据
- **Output_normalize_PLC_single_workpiece**: 数据规范化处理函数
- **get_latest_realtime_data**: 获取最新实时数据
- 其他辅助函数（Cut_data_to_Single_workpiece, index_area_For_1_4 等）

### 2. ✅ 修改的API接口

以下API接口已更新为优先使用真实数据库数据，数据库连接失败时自动回退到模拟数据：

#### 运维相关API
- ✅ `/api/maintenance/realtime` - 运维实时数据
  - 优先从 `BK_DataModel` 表获取最新数据
  - 自动检查预警条件（振动、噪声阈值）

- ✅ `/api/maintenance/predict` - 预测预警
  - 使用 `Output_normalize_PLC_single_workpiece` 获取时间段数据
  - 使用真实数据进行机器学习预测
  - 计算良率统计和预警信息

- ✅ `/api/maintenance/diagnosis` - 故障诊断
  - 获取最近1小时的规范化数据
  - 基于真实数据进行故障诊断分析

#### 监测相关API
- ✅ `/api/vibration` - 振动监测数据
  - 从数据库获取最近7天的振动数据
  - 按日期分组统计平均值

- ✅ `/api/realtime_vibration` - 实时振动监测
  - 获取最近5秒的实时振动数据
  - 显示最新的振动和噪声值

## 数据库配置

### 配置文件位置

系统会在以下位置查找数据库配置文件 `新建文本文档.ini`：

1. 项目根目录
2. `YieldModel/` 目录

### 配置文件格式

```ini
[DEFAULT]
Server=你的SQL Server地址
Database=DataAnalysis
User=用户名
Password=密码
```

### 数据库表

系统使用以下SQL Server表：

1. **BK_DataModel** - 用于获取实时数据
   - 表结构：包含 Speed_X, Speed_Y, vibration_X, vibration_Z, noise_X 等字段
   
2. **BK_DataModel_Immediate** - 用于获取时间段数据和规范化处理
   - 表结构：完整的PLC数据，包含所有字段和 creation_date

## 数据流程

```
数据库 (SQL Server)
    ↓
Connect_SQL() - 获取原始数据
    ↓
Output_normalize_PLC_single_workpiece() - 数据规范化
    ↓
API接口 - 返回给前端
```

## 错误处理

所有API接口都实现了容错机制：

1. **优先使用真实数据**：首先尝试从数据库获取数据
2. **自动回退**：如果数据库连接失败或数据不存在，自动使用模拟数据
3. **错误日志**：所有错误都会记录到控制台，方便调试

## 使用说明

### 启用数据库功能

1. 确保数据库配置文件存在且配置正确
2. 确保 `data_fetcher.py` 模块可以正常导入
3. 启动服务后，系统会自动检测并使用数据库

### 验证数据库连接

启动服务时，查看控制台输出：

- ✅ "数据获取模块加载成功，将使用真实数据库数据" - 表示数据库功能已启用
- ⚠️ "警告: 数据获取模块加载失败" - 表示将使用模拟数据

### 调试

如果数据库连接有问题，可以：

1. 检查配置文件路径和格式
2. 检查SQL Server是否可访问
3. 查看控制台的错误日志
4. 验证数据库表结构是否正确

## 注意事项

1. **数据库性能**：大量数据查询可能影响性能，建议合理设置查询时间范围
2. **数据格式**：确保数据库中的字段类型和命名与代码中的定义一致
3. **备份**：在修改数据库相关代码前，建议备份数据库配置和测试数据

## 后续优化建议

1. 添加数据库连接池以提高性能
2. 实现数据缓存机制减少数据库查询次数
3. 添加更多数据验证和清洗逻辑
4. 实现数据库连接状态监控



